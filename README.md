# Whisper Example
Basic example of how to launch a whisper instance


#1 - Some useful commands
  - `terraform plan`
  - `terraform apply`
  - `terraform destroy`

#2 - Generate ssh-key:

`$ ssh-keygen -f mykey`

